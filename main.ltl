// ltl-iir-filter/main.ltl
//
// IIR biquad filter designer — LP / HP / BP / notch.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-iir-filter v0.1.0")
    io::println("IIR biquad filter designer — LP / HP / BP / notch.")
    0
}
