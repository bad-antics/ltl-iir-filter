// examples/demo.ltl — runnable demo for ltl_iir_filter

import std::io
use ltl_iir_filter as lib

fn main() {
    io::println("ltl_iir_filter demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}